import type { SequenceManifest } from "./types";

// Empty frame lists intentionally select procedural rendering. Approved image
// sequences can be introduced here without changing any section component.
export const openingSequenceManifest: SequenceManifest = { id: "opening-family-protection-v1", frames: [] };
export const stabilizationSequenceManifest: SequenceManifest = { id: "family-reconnection-v1", frames: [] };
